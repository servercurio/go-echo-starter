package router

// PathSeparator is the URI segment delimiter used when composing module
// prefixes and route paths. Exposed as a constant so callers don't sprinkle
// string literals through path-building logic.
const PathSeparator = "/"
