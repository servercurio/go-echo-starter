// Package v1 holds the version-1 API surface. Today this is just the health
// triplet (/livez, /readyz, /healthz); add new endpoints here in their own
// files and register them in Module via additional WithRoutes entries.
package v1
