// Package route provides the standard, Echo-backed implementation of
// router.Route. Construct values with New and shape them with the Option
// helpers (WithMiddleware, WithEndpoints).
package route
